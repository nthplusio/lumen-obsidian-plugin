#!/bin/sh

# Pre-commit hook: check that help docs cover all registered commands.
# Only runs when src/ files are staged (avoids unnecessary work on non-code commits).

if git diff --cached --name-only | grep -q '^src/'; then
  node scripts/check-help-coverage.mjs
fi
